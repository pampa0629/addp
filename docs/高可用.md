基于 ADDP 的现有架构，我为你提供一份全面的高可用和弹性伸缩方案建议：
高可用和弹性伸缩架构建议
1. 服务层高可用设计
无状态服务设计（已部分实现）
✅ 当前优势：所有后端服务（System、Manager、Meta、Transfer、Gateway）都是无状态的
✅ JWT 认证无需 session 存储
📝 建议：确保所有文件上传、临时数据都存储在共享存储（MinIO）而非本地磁盘
服务多副本部署
# docker-compose.yml 扩展示例
services:
  system-backend:
    deploy:
      replicas: 3  # 运行 3 个副本
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 256M
负载均衡
                    ┌─────────────────┐
                    │  Load Balancer  │
                    │   (Nginx/HAProxy)│
                    └────────┬─────────┘
                             │
        ┌────────────────────┼────────────────────┐
        ▼                    ▼                    ▼
   ┌─────────┐         ┌─────────┐         ┌─────────┐
   │ Gateway │         │ Gateway │         │ Gateway │
   │ Instance│         │ Instance│         │ Instance│
   │    1    │         │    2    │         │    3    │
   └─────────┘         └─────────┘         └─────────┘
建议配置（nginx.conf）:
upstream gateway_backend {
    least_conn;  # 最少连接数算法
    server gateway:8000 max_fails=3 fail_timeout=30s;
    server gateway2:8000 max_fails=3 fail_timeout=30s;
    server gateway3:8000 max_fails=3 fail_timeout=30s;
}

server {
    listen 80;
    location / {
        proxy_pass http://gateway_backend;
        proxy_next_upstream error timeout http_500 http_502 http_503;
        proxy_connect_timeout 5s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
2. 数据层高可用设计
PostgreSQL 高可用方案
推荐方案：Patroni + etcd + HAProxy
┌─────────────────────────────────────────┐
│         HAProxy (5432)                  │
│  自动切换到健康的 Primary 节点           │
└──────────┬──────────────────────────────┘
           │
    ┌──────┴──────┬──────────┐
    ▼             ▼          ▼
┌─────────┐  ┌─────────┐  ┌─────────┐
│Primary  │  │Standby 1│  │Standby 2│
│(Write)  │→→│(Read)   │  │(Read)   │
└────┬────┘  └─────────┘  └─────────┘
     │
     ▼
┌─────────────────┐
│  etcd Cluster   │  (协调节点选举)
└─────────────────┘
实施步骤：
# 1. 部署 etcd 集群（3 节点）
docker-compose.yml:
  etcd1:
    image: quay.io/coreos/etcd:v3.5
  etcd2:
    image: quay.io/coreos/etcd:v3.5
  etcd3:
    image: quay.io/coreos/etcd:v3.5

# 2. 部署 Patroni + PostgreSQL
  postgres-patroni1:
    image: patroni/patroni:latest
    environment:
      PATRONI_NAME: postgres1
      PATRONI_SCOPE: addp-cluster
      PATRONI_ETCD3_HOSTS: etcd1:2379,etcd2:2379,etcd3:2379

# 3. 部署 HAProxy（连接池 + 自动切换）
  haproxy:
    image: haproxy:2.8
    ports:
      - "5432:5432"  # Primary (写)
      - "5433:5433"  # Standbys (读)
备选方案（云环境）:
AWS RDS Multi-AZ
Google Cloud SQL with HA
Azure Database for PostgreSQL with HA
Redis 高可用方案
推荐：Redis Sentinel 模式
services:
  redis-master:
    image: redis:7-alpine
    command: redis-server --appendonly yes

  redis-replica1:
    image: redis:7-alpine
    command: redis-server --slaveof redis-master 6379

  redis-replica2:
    image: redis:7-alpine
    command: redis-server --slaveof redis-master 6379

  redis-sentinel1:
    image: redis:7-alpine
    command: redis-sentinel /etc/redis/sentinel.conf
  
  redis-sentinel2:
    image: redis:7-alpine
    command: redis-sentinel /etc/redis/sentinel.conf
  
  redis-sentinel3:
    image: redis:7-alpine
    command: redis-sentinel /etc/redis/sentinel.conf
代码适配（Go Redis 客户端）:
import "github.com/redis/go-redis/v9"

rdb := redis.NewFailoverClient(&redis.FailoverOptions{
    MasterName:    "mymaster",
    SentinelAddrs: []string{
        "sentinel1:26379",
        "sentinel2:26379",
        "sentinel3:26379",
    },
})
MinIO 高可用方案
推荐：MinIO 分布式模式（4 节点最小）
services:
  minio1:
    image: minio/minio
    command: server http://minio{1...4}/data{1...2}
    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: password

  minio2:
    image: minio/minio
    command: server http://minio{1...4}/data{1...2}

  minio3:
    image: minio/minio
    command: server http://minio{1...4}/data{1...2}

  minio4:
    image: minio/minio
    command: server http://minio{1...4}/data{1...2}

  # Nginx 负载均衡 MinIO 节点
  minio-lb:
    image: nginx:alpine
    ports:
      - "9000:9000"
3. 弹性伸缩设计
Kubernetes 部署（推荐生产环境）
优势：
✅ 自动故障恢复（Pod 重启）
✅ 水平自动扩缩容（HPA）
✅ 滚动更新零停机
✅ 服务发现和负载均衡
架构示意：
┌───────────────────────────────────────────────┐
│          Kubernetes Cluster                   │
│                                               │
│  ┌─────────────────────────────────────────┐ │
│  │  Ingress Controller (Nginx/Traefik)    │ │
│  └──────────────┬──────────────────────────┘ │
│                 │                             │
│  ┌──────────────┴──────────────┐             │
│  │   Gateway Service (ClusterIP)│             │
│  │   HPA: min=2, max=10         │             │
│  └──────────────┬──────────────┘             │
│                 │                             │
│  ┌──────────────┴──────────────┐             │
│  │  Backend Services            │             │
│  │  - System (HPA: 2-5)         │             │
│  │  - Manager (HPA: 2-8)        │             │
│  │  - Meta (HPA: 1-5)           │             │
│  │  - Transfer (HPA: 2-10)      │             │
│  └──────────────────────────────┘             │
│                                               │
│  ┌──────────────────────────────┐             │
│  │  StatefulSet                 │             │
│  │  - PostgreSQL (3 replicas)   │             │
│  │  - Redis (3 replicas)        │             │
│  │  - MinIO (4 replicas)        │             │
│  └──────────────────────────────┘             │
└───────────────────────────────────────────────┘
HPA 配置示例 (k8s/gateway-hpa.yaml):
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: gateway-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: gateway
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
      - type: Percent
        value: 50
        periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Pods
        value: 1
        periodSeconds: 60
服务监控指标（用于自动扩缩容）
建议添加 Prometheus 监控:
// 在每个服务的 main.go 中添加 Prometheus metrics
import (
    "github.com/prometheus/client_golang/prometheus"
    "github.com/prometheus/client_golang/prometheus/promhttp"
)

var (
    httpRequestsTotal = prometheus.NewCounterVec(
        prometheus.CounterOpts{
            Name: "http_requests_total",
            Help: "Total number of HTTP requests",
        },
        []string{"method", "endpoint", "status"},
    )
    
    httpRequestDuration = prometheus.NewHistogramVec(
        prometheus.HistogramOpts{
            Name:    "http_request_duration_seconds",
            Help:    "HTTP request latency",
            Buckets: prometheus.DefBuckets,
        },
        []string{"method", "endpoint"},
    )
)

func init() {
    prometheus.MustRegister(httpRequestsTotal)
    prometheus.MustRegister(httpRequestDuration)
}

// 在 router 中添加 metrics endpoint
router.GET("/metrics", gin.WrapH(promhttp.Handler()))
4. 配置中心高可用
当前 ADDP 使用 System 模块作为配置中心，需要增强其可用性：
方案 A：配置中心多副本 + 数据库读写分离
// common/config/loader.go 支持多个 System 实例
func LoadSharedConfig() (*SharedConfig, error) {
    systemURLs := []string{
        os.Getenv("SYSTEM_SERVICE_URL_1"),
        os.Getenv("SYSTEM_SERVICE_URL_2"),
        os.Getenv("SYSTEM_SERVICE_URL_3"),
    }
    
    for _, url := range systemURLs {
        config, err := fetchConfigFromSystem(url)
        if err == nil {
            return config, nil
        }
        log.Printf("Failed to fetch from %s, trying next...", url)
    }
    
    // Fallback to local .env
    return loadFromEnv(), nil
}
方案 B：使用专业配置中心（推荐生产环境）
Consul - 服务发现 + KV 存储 + 健康检查
etcd - 强一致性 KV 存储
Nacos - 阿里开源，支持配置管理 + 服务发现
5. 数据备份与灾难恢复
自动备份策略
# 添加到 cron job (scripts/backup-cron.sh)
#!/bin/bash

# PostgreSQL 备份（每天凌晨 2 点）
BACKUP_DIR="/backups/postgres/$(date +%Y%m%d)"
mkdir -p $BACKUP_DIR

# 备份 ADDP 系统数据库
docker exec postgres pg_dump -U addp addp > $BACKUP_DIR/addp_system.sql

# 备份业务数据库
docker exec business-postgres pg_dump -U addp business > $BACKUP_DIR/business_data.sql

# MinIO 数据同步到异地对象存储
mc mirror --remove minio/addp-bucket s3/backup-bucket/

# 保留最近 7 天的备份
find /backups/postgres -mtime +7 -exec rm -rf {} \;
异地多活架构（终极高可用）
       ┌─────────────────────────────────┐
       │   Global Load Balancer (DNS)    │
       └───────────┬─────────────────────┘
                   │
       ┌───────────┴─────────────┐
       ▼                         ▼
┌──────────────┐          ┌──────────────┐
│  Region A    │          │  Region B    │
│  (Primary)   │  ←───→   │  (Standby)   │
│              │          │              │
│  Full Stack  │  Sync    │  Full Stack  │
└──────────────┘          └──────────────┘
6. 实施路线图建议
第一阶段：基础高可用（1-2 周）
✅ 为所有后端服务添加健康检查接口 /health
✅ 配置 Docker Compose restart policies
✅ 部署 Nginx 作为负载均衡器
✅ PostgreSQL 主从复制（一主一从）
✅ Redis Sentinel 模式
第二阶段：弹性伸缩（2-3 周）
✅ 添加 Prometheus + Grafana 监控
✅ 迁移到 Kubernetes
✅ 配置 HPA（水平自动扩缩容）
✅ 部署 MinIO 分布式集群
第三阶段：生产级增强（持续优化）
✅ 配置 PostgreSQL 高可用集群（Patroni）
✅ 接入专业配置中心（Consul/Nacos）
✅ 实施自动备份和恢复测试
✅ 建立异地灾备方案
7. 关键代码改造建议
数据库连接池优化
// internal/repository/database.go
func InitDB(dsn string) (*gorm.DB, error) {
    db, err := gorm.Open(postgres.Open(dsn), &gorm.Config{})
    if err != nil {
        return nil, err
    }
    
    sqlDB, _ := db.DB()
    sqlDB.SetMaxOpenConns(100)     // 最大连接数
    sqlDB.SetMaxIdleConns(10)      // 最大空闲连接数
    sqlDB.SetConnMaxLifetime(time.Hour) // 连接最大生命周期
    
    return db, nil
}
优雅关闭（避免请求丢失）
// cmd/server/main.go
func main() {
    router := setupRouter()
    
    srv := &http.Server{
        Addr:    ":8080",
        Handler: router,
    }
    
    go func() {
        if err := srv.ListenAndServe(); err != nil && err != http.ErrServerClosed {
            log.Fatalf("listen: %s\n", err)
        }
    }()
    
    // 优雅关闭
    quit := make(chan os.Signal, 1)
    signal.Notify(quit, syscall.SIGINT, syscall.SIGTERM)
    <-quit
    
    log.Println("Shutting down server...")
    ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
    defer cancel()
    
    if err := srv.Shutdown(ctx); err != nil {
        log.Fatal("Server forced to shutdown:", err)
    }
    
    log.Println("Server exiting")
}
总结
基于 ADDP 当前架构，建议： 快速见效（本周可实施）:
✅ 为所有服务添加健康检查
✅ 配置 Nginx 负载均衡器
✅ PostgreSQL 一主一从复制
✅ Docker Compose 自动重启策略
中期目标（1 个月）:
✅ 迁移到 Kubernetes
✅ 配置自动扩缩容（HPA）
✅ 部署 Prometheus 监控
长期建设（持续优化）:
✅ PostgreSQL 高可用集群
✅ 多区域部署
✅ 自动备份和灾备演练
需要我详细展开某个方案的具体实施步骤吗？例如：
Kubernetes 部署 YAML 配置
PostgreSQL Patroni 配置
Prometheus 监控配置
自动备份脚本