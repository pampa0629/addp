我正在努力，把 addp 改造为一个让 用户用自然语言输入要求，然后AI agent根据addp的能力，自动完成要求的任务。
最简单的任务是：用data目录下的数据，计算铁路两边五十米的耕地面积。
大致分两个阶段：
1，看当前的addp的能力，能否完成相关的任务；对于不适合的地方做改造；
2，提供一个新的自然语言的入口，以后就用这个入口来搞定任务。

你先根据这个简单任务，分析一下addp的能力有哪些不足和需要改造的地方。