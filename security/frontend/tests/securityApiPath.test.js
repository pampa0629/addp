import { describe, expect, it, vi } from 'vitest'
vi.mock('../src/api/client', () => ({ default: { get: vi.fn(path => path), post: vi.fn((path, data) => ({path,data})), put: vi.fn((path,data)=>({path,data})), delete: vi.fn((path,config)=>config?{path,config}:path) } }))
import client from '../src/api/client'
import { assessmentAPI, classificationAPI, definitionProfileAPI, detectorAPI, detectorCapabilityAPI, discoveryQualityAPI, findingAPI, gradeAPI, sensitiveDataTypeAPI, protectionBaselineAPI, protectionEnrollmentAPI, protectionExemptionAPI, metaAPI } from '../src/api/security'
describe('Security API paths', () => { it('uses the single /security route family', () => { expect(classificationAPI.list()).toBe('/security/classifications'); expect(gradeAPI.get(2)).toBe('/security/grades/2'); expect(sensitiveDataTypeAPI.delete(3)).toBe('/security/sensitive-data-types/3'); expect(definitionProfileAPI.list()).toBe('/security/definition-profiles'); expect(definitionProfileAPI.apply('recommended').path).toBe('/security/definition-profile-applications'); expect(detectorCapabilityAPI.list()).toBe('/security/detector-capabilities'); expect(detectorAPI.create({ capability_key: 'x' }).path).toBe('/security/detectors'); expect(detectorAPI.delete(8,{version:2}).config.data).toEqual({version:2}); const qualityParams = { sensitive_data_type_id: 9 }; discoveryQualityAPI.get(qualityParams); expect(client.get).toHaveBeenLastCalledWith('/security/discovery-quality', { params: qualityParams }); expect(protectionBaselineAPI.update(4,{}).path).toBe('/security/protection-baselines/4'); expect(protectionBaselineAPI.delete(4,{version:2}).config.data).toEqual({version:2}); expect(protectionEnrollmentAPI.list()).toBe('/security/protection-enrollments'); const listParams = { scope: 'released', page: 2, page_size: 20 }; protectionEnrollmentAPI.list(listParams); expect(client.get).toHaveBeenLastCalledWith('/security/protection-enrollments', { params: listParams }); const findingParams = { snapshot_scope: 'current', review_state: 'pending' }; findingAPI.list(findingParams); expect(client.get).toHaveBeenLastCalledWith('/security/findings', { params: findingParams }); expect(protectionEnrollmentAPI.components('enrollment-1')).toBe('/security/protection-enrollments/enrollment-1/components'); const manual = assessmentAPI.create({ enrollment_id: 'enrollment-1' }); expect(manual.path).toBe('/security/assessments'); const revoke = assessmentAPI.revoke('assessment-1',{version:2,rationale:'verified'}); expect(revoke.path).toBe('/security/assessments/assessment-1'); expect(revoke.config.data).toEqual({version:2,rationale:'verified'}); const exemption = protectionExemptionAPI.create({ assessment_id: 'assessment-1' }); expect(exemption.path).toBe('/security/protection-exemptions'); const renewExemption = protectionExemptionAPI.renew('exemption-1',{version:2}); expect(renewExemption.path).toBe('/security/protection-exemptions/exemption-1'); const revokeExemption = protectionExemptionAPI.revoke('exemption-1',{version:3,rationale:'done'}); expect(revokeExemption.config.data).toEqual({version:3,rationale:'done'}); const reenrollment = protectionEnrollmentAPI.reEnroll('enrollment-1',{version:5}); expect(reenrollment.path).toBe('/security/protection-enrollments/enrollment-1/re-enrollments'); expect(reenrollment.data).toEqual({version:5}); const release = protectionEnrollmentAPI.release('enrollment-1',{version:4,basis:'no_supported_findings',reason:'reviewed'}); expect(release.path).toBe('/security/protection-enrollments/enrollment-1/releases'); expect(release.data).toEqual({version:4,basis:'no_supported_findings',reason:'reviewed'}); expect(protectionEnrollmentAPI.rediscover('enrollment-1',{version:3}).path).toBe('/security/protection-enrollments/enrollment-1/discovery-executions'); expect(metaAPI.getItem(51657)).toBe('/meta/items/51657') }) })
