import { describe, expect, it, vi } from 'vitest'
vi.mock('../src/api/client', () => ({ default: { get: vi.fn(path => path), post: vi.fn((path, data) => ({path,data})), put: vi.fn((path,data)=>({path,data})), delete: vi.fn((path,config)=>config?{path,config}:path) } }))
import { classificationAPI, gradeAPI, sensitiveDataTypeAPI, protectionBaselineAPI, protectionEnrollmentAPI, metaAPI } from '../src/api/security'
describe('Security API paths', () => { it('uses the single /security route family', () => { expect(classificationAPI.list()).toBe('/security/classifications'); expect(gradeAPI.get(2)).toBe('/security/grades/2'); expect(sensitiveDataTypeAPI.delete(3)).toBe('/security/sensitive-data-types/3'); expect(protectionBaselineAPI.update(4,{}).path).toBe('/security/protection-baselines/4'); expect(protectionBaselineAPI.delete(4,{version:2}).config.data).toEqual({version:2}); expect(protectionEnrollmentAPI.list()).toBe('/security/protection-enrollments'); const release = protectionEnrollmentAPI.release('enrollment-1',{version:4,basis:'no_supported_findings',reason:'reviewed'}); expect(release.path).toBe('/security/protection-enrollments/enrollment-1/releases'); expect(release.data).toEqual({version:4,basis:'no_supported_findings',reason:'reviewed'}); expect(protectionEnrollmentAPI.rediscover('enrollment-1',{version:3}).path).toBe('/security/protection-enrollments/enrollment-1/discovery-executions'); expect(metaAPI.getItem(51657)).toBe('/meta/items/51657') }) })
