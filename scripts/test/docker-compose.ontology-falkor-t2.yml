services:
  falkordb:
    extends:
      file: ../infra/falkordb.yml
      service: falkordb
    ports:
      - "127.0.0.1::6379"
    volumes:
      - falkordb_data:/data
    restart: "no"
volumes:
  falkordb_data:
