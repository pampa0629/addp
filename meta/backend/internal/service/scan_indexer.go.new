package service

import (
	"log/slog"

	commonModels "github.com/addp/common/models"
	"github.com/addp/meta/internal/models"
	"github.com/addp/meta/internal/search"
	"github.com/addp/meta/plugins"
)

// ScanIndexerService 搜索索引服务
type ScanIndexerService struct {
	log     *slog.Logger
	indexer *search.Indexer
}

// NewScanIndexerService 创建索引服务
func NewScanIndexerService(log *slog.Logger, indexer *search.Indexer) *ScanIndexerService {
	return &ScanIndexerService{
		log:     log,
		indexer: indexer,
	}
}

// IndexTableAsset 索引表资产（占位实现）
func (s *ScanIndexerService) IndexTableAsset(
	resource *commonModels.Engine,
	tenantID uint,
	schemaName string,
	tableInfo plugins.TableInfo,
	fields []plugins.FieldInfo,
	item *models.MetaItem,
) {
	// 占位实现
}

// DeleteTablesFromIndex 从索引中删除表（占位实现）
func (s *ScanIndexerService) DeleteTablesFromIndex(tenantID, engineID uint, schemaName string) {
	// 占位实现
}
