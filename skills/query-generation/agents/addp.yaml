schema: addp.skill-runtime/v1
required_skills:
  - data-discovery
required_tools:
  - engine.list
  - data.search
  - resource.ancestors.get
  - data.preview
  - query.draft.generate
max_iterations: 7
