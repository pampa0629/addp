server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ^~ /inference/assets/ {
        rewrite ^/inference/(.*)$ /$1 break;
        try_files $uri =404;
    }
}
