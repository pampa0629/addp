# 简化版 Dockerfile - 用于快速测试大小
FROM centos:7

WORKDIR /app

# 安装 SQLite
RUN yum install -y sqlite && yum clean all

# 复制预编译的二进制文件 (从 backend 目录构建)
# 需要先把 server 复制到 backend 目录
COPY server .

# 创建数据目录
RUN mkdir -p /app/data

EXPOSE 8080

CMD ["./server"]