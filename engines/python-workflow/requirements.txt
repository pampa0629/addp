# Python Workflow Engine Dependencies

# Core spatial computation. Keep NumPy below 2.3 so local GDAL Python bindings
# inherited from system site-packages remain import-compatible.
numpy>=1.26,<2.3
pandas>=2.0,<3.0
geopandas>=0.15.0
shapely>=2.1.0
pyproj>=3.6.0
pyarrow>=18.0.0

# Web framework
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# Database connectivity
psycopg2-binary==2.9.9
pymysql==1.1.0
sqlalchemy==2.0.23
geoalchemy2==0.14.3

# HTTP client for System registration
requests==2.31.0

# Data validation and settings management
pydantic>=2.0

# Environment variable loading
python-dotenv>=1.0.0

# Safe expression evaluation for calculate_field operator
simpleeval==0.9.13

# JSON handling (included in Python stdlib, but explicit for clarity)
# json, uuid, datetime are stdlib modules
