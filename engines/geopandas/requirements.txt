# GeoPandas Engine Dependencies

# Core spatial computation (with NumPy 1.x compatibility)
numpy<2.0
pandas>=2.0,<3.0
geopandas==0.14.1
shapely==2.0.2

# Web framework
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# Database connectivity
psycopg2-binary==2.9.9
pymysql==1.1.0
sqlalchemy==2.0.23
geoalchemy2==0.14.3

# HTTP client for System registration
requests==2.31.0

# Data validation and settings management
pydantic>=2.0

# Safe expression evaluation for calculate_field operator
simpleeval==0.9.13

# JSON handling (included in Python stdlib, but explicit for clarity)
# json, uuid, datetime are stdlib modules
